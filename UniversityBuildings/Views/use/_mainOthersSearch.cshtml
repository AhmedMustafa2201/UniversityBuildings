@model IEnumerable<UniversityBuildings.Models.Other_Buildings>
@{
    var currentlg = ViewBag.currentLanguage;
}

@{
    if (currentlg == "en")
    {
        <div class="container">
            <div style="align-items:center;margin-top:2rem" class="row">
                @foreach (var item in Model)
                {
                    <div class="col-md-4">
                        <div style="margin-bottom:2rem" class="card border-info">
                            <div class="card-body">
                                <h3 class="card-title">@Html.DisplayFor(modelItem => item.Building_NameEN)</h3>
                                <p class="card-text">@Html.DisplayFor(modelItem => item.Building_ServicesEN)</p>
                                <p class="card-text">@Html.DisplayFor(modelItem => item.Building_Responsible_NameEN)</p>
                                <a href="~/use/OtherBuildingLearnMore?id=@item.ID" class="btn btn-outline-success">Learn More <i class="fa fa-newspaper-o"></i></a>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    }
    else
    {
        <div class="container">
            <div style="align-items:center;direction:rtl; margin-top:2rem" class="row">
                @foreach (var item in Model)
                {
                    <div class="col-md-4">
                        <div style="margin-bottom:2rem" class="card border-info text-right">
                            <div class="card-body">
                                <h3 style="font-family:'Droid Arabic Kufi'" class="card-title">@Html.DisplayFor(modelItem => item.Building_Name)</h3>
                                <p style="font-family:'Droid Arabic Kufi'" dir="rtl" class="card-text">@Html.DisplayFor(modelItem => item.Building_Services)</p>
                                <p style="font-family:'Droid Arabic Kufi'" dir="rtl" class="card-text">@Html.DisplayFor(modelItem => item.Building_Responsible_Name)</p>
                                <a style="font-family:'Droid Arabic Kufi'" href="~/use/OtherBuildingLearnMore?id=@item.ID" class="btn btn-outline-success">معرفة المزيد <i class="fa fa-newspaper-o"></i></a>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    }
}
